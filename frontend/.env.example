# The frontend is a self-contained demo on mock data (frontend/lib/demoData.mjs).
# It needs NO environment variables and never connects to a backend or real store.
# `npm run dev` and a production build both just work with no config.
