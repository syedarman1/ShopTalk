# Port for the API + SSE + MCP server. Most hosts inject PORT automatically.
PORT=4000

# Path to the SQLite database file. In production point this at a mounted
# persistent volume so data survives restarts/redeploys (e.g. /data/mockbase.db).
MOCKBASE_DB=./mockbase.db
